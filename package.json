{
  "name": "grt-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ roman.brodetski/grt-token",
    "create-local": "graph create --node http://localhost:8020/ roman.brodetski/grt-token",
    "remove-local": "graph remove --node http://localhost:8020/ roman.brodetski/grt-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 roman.brodetski/grt-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
